import * as types from './../../constants/ActionType';

let initialState = [];
const ThongKe = (state = initialState, action)=>{
    switch (action.type) {

        default: return state;
    }
}

export default ThongKe;